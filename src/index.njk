---
layout: layout1
title: "The Node.js Italian Conference 2023"
description: "The Italian Nodejsconf is back!"
url: "https://2023.nodejsconf.it"
facebook_post: "https://2023.nodejsconf.it/assets/facebook_post.png"
twitter_post: "https://2023.nodejsconf.it/assets/twitter_post.png"
twitter: "webdebresa"
permalink: '/'
---

<section id="conference" class="row">
  <h1>The international Nodejs Conference in Italy
  </h1>
  <h4>
    30th September 2023 - Brescia, Italy</h4>
    <div class="row">
      <div class='conference-section'> 
        <div class="pt-4"> {% renderFile "./src/contents/conference.md" %} </div>
        <div> 
          <section id="venue">
            {% renderFile "./src/contents/venue.md" %}
          </section>
          <section id="tickets">
            {% renderFile "./src/contents/tickets.md" %}
          </section>
        
      </div>
    </div>
  </section>

  <div id="map-venue" class="mb-5"></div> 
  {#
    <section id="cfp" class='cfp'>
      {% renderFile "./src/contents/cfp.md" %}
      <a
        href="https://docs.google.com/forms/d/1qWxi6NRLyoroqqy3w7bDyGilO0QdN-xsTc6r4SHAx9Q/edit"
        class="link-button"
      >Submit your proposal</a>
    </section>
  #}
  
  <section id="schedule">
    <div class="intro mb-5">
      {% renderFile "./src/contents/schedule.md" %}
    </div>
    <ul class="schedule-slots">
      {%- for slot in collections.slots -%}
      <li class="slot">
        <div class="timeslot">{{ slot.data.timeslot }}</div>
        <a 
          class="info {{ slot.data.type }}" 
          {% if slot.data.type == 'talk' %}
          href="#{{ slot.url | replace("/", "_")  }}"
          {% endif %}
        >
          <div class="title">{{ slot.data.title }}</div>
          <div class="author">{{ slot.data.author_name }}</div>
        </a>
      </li>
      {%- endfor -%}
    </ul>
  </section>

  <section id="talks">
    <div class="intro mb-5">
      {% renderFile "./src/contents/talks.md" %}
    </div>
    {%- for talk in collections.talks -%}
    <div class="row" id="{{ talk.url | replace("/", "_")  }}">
      <div class="col-12">
        <h3>{{ talk.data.title }}</h3>
        <div class="talk-topics mb-4 ">{{ talk.data.topics | safe }}</div>
        <div class="talk-abstract mb-4">
          {{ talk.templateContent | safe }}
        </div>
        <div class="author mb-2">
          <div class="row">
            <div class="col-12">
              <div class="author-info">
                <div class="d-flex align-items-center ">
                  <div class="author-image mr-4">
                    <img class="d-block img-fluid" src="{{ talk.data.author_image_url }}"
                      alt="{{ talk.data.author_name }} picture">
                  </div>
                  <div>
                    <div class="author-name">{{ talk.data.author_name | safe }}
                      <div class="author-links">
                        {%- for link in talk.data.author_links -%}
                        <a class="d-inline-block m-1" target="_blank" href="{{ link.url }}"
                          alt="{{ talk.data.author_name }} {{ link.name }}">
                          <i class="bi bi-{{ link.icon }}"></i>
                        </a>
                        {%- endfor -%}
                      </div>
                    </div>
                    <div class="author-info">{{ talk.data.author_info | safe }}</div>
                  </div>
                </div>
                <div class="author-bio">{{ talk.data.author_bio | safe }}</div>
              </div>
  
            </div>
          </div>
        </div>
      </div>
    </div>
    <hr>
    {%- endfor -%}
  </section>

  <section id="sponsorship" class="mb-24">
    {% renderFile "./src/contents/sponsor.md" %}
    <div class="row">
      {%- for post in collections.sponsor_packages -%}
        <div class="col-12 col-md-6 col-lg-3 g-4">
          <div class="p-4 h-100 price-card">
            <h3 class="mb-0">{{ post.data.title }}</h3>
            <div class="price">
              <div class="fs-3">{{ post.data.price }} &euro;</div>
            </div>
            <hr>
              {{ post.templateContent | safe }} 
            </div>
          </div>
        {%- endfor -%}
      </div>
    </section>

    <section id="sponsors">
      {% renderFile "./src/contents/sponsors.md" %}
      <div id="sponsors-platinum" class="sponsor-level mb-4">
        {%- for sponsor in collections.sponsors_platinum -%}
          <a href="{{ sponsor.data.url }}" target="_blank" class="sponsor">
            <div class="sponsor-image sponsor-level-{{sponsor.data.level}}">
              <img src="{{ sponsor.data.image }}"> </img>
              <div class="badge">{{ sponsor.data.level }}</div>
            </div>
          </a>
        {%- endfor -%}
      </div>
      <div id="sponsors-silver" class="sponsor-level mb-4">
        {%- for sponsor in collections.sponsors_gold -%}
          <a href="{{ sponsor.data.url }}" target="_blank" class="sponsor">
            <div class="sponsor-image sponsor-level-{{sponsor.data.level}}">
              <img src="{{ sponsor.data.image }}"> </img>
              <div class="badge">{{ sponsor.data.level }}</div>
            </div>
          </a>
        {%- endfor -%}
      </div>
      <div id="sponsors-silver" class="sponsor-level mb-4">
        {%- for sponsor in collections.sponsors_silver -%}
          <a href="{{ sponsor.data.url }}" target="_blank" class="sponsor">
            <div class="sponsor-image sponsor-level-{{sponsor.data.level}}">
              <img src="{{ sponsor.data.image }}"> </img>
              <div class="badge">{{ sponsor.data.level }}</div>
            </div>
          </a>
        {%- endfor -%}
      </div>
      <div id="sponsors-bronze" class="sponsor-level mb-4">
        {%- for sponsor in collections.sponsors_bronze -%}
          <a href="{{ sponsor.data.url }}" target="_blank" class="sponsor">
            <div class="sponsor-image sponsor-level-{{sponsor.data.level}}">
              <img src="{{ sponsor.data.image }}"> </img>
              <div class="badge">{{ sponsor.data.level }}</div>
            </div>
          </a>
        {%- endfor -%}
      </div>
      <div class="mt-24">
        <a class="link-button" href="mailto:info@webdebs.org?subject=Sponsorship request">Become a sponsor</a>
      </div>
    </section>

    <section id="media_partners">
      {% renderFile "./src/contents/media_partners.md" %}
      <div class="mb-4">
        {%- for media_partner in collections.media_partners -%}
          <a href="{{ media_partner.data.url }}" target="_blank" class="media_partner">
            <div class="media_partner-image">
              <img src="{{ media_partner.data.image }}"> </img>
            </div>
          </a>
        {%- endfor -%}
      </div>
    </section>

    <div class='banner mt-24'></div>

    <section id="follow-us" class="mt-96 text-center">
      <h1>
        👀 Follow us</h1>
      <div class="d-flex justify-content-center">
          <a class="d-block m-2" href="https://twitter.com/webdebresa">
            <i class="bi bi-twitter"></i>
            Twitter
          </a>
          <a class="d-block m-2" href="https://facebook.com/webdebs">
            <i class="bi bi-facebook"></i>
            Facebook
          </a>
          <a class="d-block  m-2" href="https://webdebs.eventbrite.com">
            <i class="bi bi-calendar-event"></i>
            Eventbrite
          </a>
        <!-- div class="col"> <a href="http://slack.webdebs.org">
            <i class="bi bi-slack"></i> Slack
        </a> </div> -->
      </div>
    </section>
    <section id="contacts" class="text-center">
      <h1 >
        🤙 Contacts</h1>
      <div class="d-flex justify-content-center">
          <a class="d-block m-2" href="http://webdebs.org">
            <i class="bi bi-globe"></i>
            Website
          </a>
          <a class="d-block m-2" href="mailto:info@webdebs.org">
            <i class="bi bi-envelope"></i>
            Email
          </a>
      </div>
    </section>
